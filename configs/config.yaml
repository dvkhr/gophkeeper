server:
  port: 50051
  mode: development

database:
  dsn:  "host=localhost port=5432 user=postgres password=postgres dbname=gophkeeper sslmode=disable"

auth:
  jwt_secret: super-secret-key
  jwt_ttl_hours: 72
  refresh_token_ttl_days: 7